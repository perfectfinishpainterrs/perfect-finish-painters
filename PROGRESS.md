# Progress — Perfect Finish Painters

## Shipped
- [x] 2026-06-19 — **Fixed mis-pointed legacy redirect `/deck-fence-staining` (GSC triage).** GSC "Crawled – not indexed" flagged the old combined URL `/deck-fence-staining`, which `next.config.ts` was 301-ing to the unrelated `/shed-restoration`. The service was already correctly split into two dedicated, fully-wired pages — `/deck-staining` and `/fence-staining` (both in sitemap + nav + services list) — so nothing was actually missing; only the legacy URL was sent to the wrong place. Re-pointed the redirect to `/deck-staining` (primary term; it cross-links fence staining). No other code referenced the dead slug. The other GSC flags reviewed same session were all benign noise: the 404 was a stale Next.js font hash from a redeploy; "Page with redirect" was the http/www→canonical variants (correct); `/wallpaper-removal` is a healthy 989-word page just awaiting indexing (Request Indexing + internal links).
- [x] 2026-06-12 — **Header decluttered + hero estimate line repositioned (BP visual feedback).** Moved "Get Your Free Estimate in 60 Seconds" out of the H1 to below the hero tough-jobs card. Fixed the overcrowded top nav: `whitespace-nowrap` on links (Our Work was wrapping), switched the desktop nav→hamburger breakpoint from `md`→`lg` (was cramming at tablet widths), gated the phone to `xl` + nowrap (was colliding with Contact/ES and wrapping to 3 lines), removed the WhatsApp button from the top row (still in the mobile menu), and trimmed the oversized logo + added a gap before the nav. Verified clean at 1100 / 1280 / 1440; hamburger below 1024. Deployed to master (Vercel).
- [x] 2026-06-12 — **Split hero with a "Tough Jobs We Tackle" showcase next to the brand.** BP wanted hard jobs front-and-center in the hero. Rebuilt the homepage hero from a centered full-bleed layout into a two-column split: brand headline + CTA + trust/license badges on the left, a `ToughJobsCard` before/after card on the right (navy gradient bg). New `ToughJobsCard` client component + `src/data/tough-jobs.ts` data file — **data-driven and rotation-ready**: each new tough job is one entry in `toughJobs[]` and the card auto-rotates (5s) once there's more than one; dots are real `<button>`s with aria-labels. Seeded with the Mays Landing full ceiling rebuild (before/after, links to `/popcorn-ceiling-removal`). This finally surfaces the ceiling job as a real highlight (it was previously only mid-page on the service + city pages). Hero LCP preload re-pointed to the ceiling after-image. Build passes 64/64; verified at desktop + mobile widths (`reports/hero-desktop.png`). To add the next tough job: drop an entry in `tough-jobs.ts` (before/after webp in `public/projects/`) — nothing else.
- [x] 2026-06-12 — **Founder identity completed — Brandon Panameno.** Updated the 10 blog `author` Person schemas from "Brandon" → "Brandon Panameno". Added a `Person` entity on the About page (`@id .../about#brandon`, jobTitle Owner, `worksFor` → business `@id`, image owner.webp) so every blog post's author link now resolves to a real, machine-readable founder entity (closes the "no Person schema on About" GEO/Content drag). Updated the visible About bio + H2 to the full name so rendered content matches the schema. Build passes, 64/64 pages.
- [x] 2026-06-12 — **Rolled in the optional polish (real-world quality beyond the rubric).** (1) **OG social card:** generated a branded 1200×630 `public/og-card.jpg` (shed before/after photo + navy strip + "PERFECT FINISH PAINTERS" wordmark + phone/URL, 166 KB) and pointed every `openGraph`/`twitter` image at it across 29 files — replaced the old 1970×748 logo that cropped badly in link previews. Publisher-logo schema refs left on `logo.webp` (correct). (2) **Person author schema:** 10 blog posts switched from `author: Organization` → `author: { Person, name: "Brandon", url: /about }` for EEAT (used first name only — no surname on file; add later to strengthen). (3) **Hero preload:** added an explicit `<link rel="preload" as="image" fetchPriority="high">` for the home hero on `page.tsx` (next/image `fill` doesn't reliably emit one). Production build passes, all 64 static pages generate. Rubric score holds at **96** (OG presence was already credited) — these improve real-world sharing, AI attribution, and LCP beyond what the rubric measures.
- [x] 2026-06-12 — **Cleared the remaining score levers → 96 (Excellent).** (1) **Trust headers:** added `Permissions-Policy: camera=(), microphone=(), geolocation=()` + `Strict-Transport-Security: max-age=31536000; includeSubDomains` to `next.config.ts`. (2) **Orphan cleanup:** deleted 36 unreferenced `.jpg/.png` originals from `public/` (18 MB freed; each had a `.webp` sibling + zero `src/` refs; verified nothing in manifests/metadata pointed at them) — `public/` is now WebP-only. (3) **H2 question sweep:** converted 23 statement H2s to question form across 3 service pages + 7 blog posts (e.g. "The Problem With Flat" → "What's the Problem With Flat Paint?"); site-wide H2 question ratio 58% → **71%** (remaining non-question H2s are legal/contact/about, correctly left as statements). Production `npm run build` passes clean. Re-scored: SEO 98, GEO 92, Perf 96, A11y 98, Trust 98, Content 93 → **aggregate 96**. PDF refreshed.
- [x] 2026-06-12 — **Reconciled the site score to 95 (Excellent) — the morning "74" was a scoring-methodology artifact, not a regression.** The full-audit run earlier today used 4 parallel audit-agents that scored ~20 pts harder than the `site-score` rubric by inventing deductions the rubric doesn't define (OG-image *quality* vs. mere presence, cookie-consent banner, Person schema, `sizes` attr, carousel dots, over-weighted orphan images). Compared dimension-by-dimension to the 2026-05-24 baseline (96) and confirmed only **two genuine regressions** had crept in since: (1) 5 newer blog posts were never added to `sitemap.ts` (10 folders, 5 listed) — **fixed**, all 10 now listed; (2) 4 OurWorkGallery images shipped oversized (1.4/1.28/1.17 MB + 528 KB, 2560px) — **fixed**, recompressed to 1280px q80 = 4.4 MB → 244 KB. Plus the reviewCount fix (5.0/29) above. Re-scored against the real rubric: SEO 98, GEO 90, Perf 95, A11y 98, Trust 98, Content 93 → **aggregate 95**. Regenerated `reports/perfect-finish-painters-site-score-2026-06-12.pdf` (supersedes the 74 PDF). **Lesson for future audits: the audit skills must score the site-score rubric items as written and not layer on extra criteria, or the trend line breaks.**
- [x] 2026-06-12 — **Fixed the aggregateRating manual-action risk — now matches the real Google Business Profile (5.0 / 29).** Home page hardcoded `reviewCount: "19"` and `/reviews` used `reviews.length` (12) — both wrong and inconsistent for the same `@id` entity. BP confirmed live GBP = **5.0 stars, 29 Google reviews**. Added `GOOGLE_RATING="5.0"` + `GOOGLE_REVIEW_COUNT="29"` as a single source of truth in `src/data/reviews.ts` (the `reviews` array stays a curated on-page sample); both `src/app/(en)/page.tsx` and `src/app/(en)/reviews/page.tsx` now import them. Schema is truthful + verifiable against the GBP BP manages. Typecheck clean. Lifts GEO ~71→~76 (removes the −5 deduction) on next score run.
- [x] 2026-06-12 — **Full audit (SEO·GEO·Perf·A11y·Trust·Content) + site score.** Ran all four hardened audit skills as parallel agents → aggregated to `reports/perfect-finish-painters-site-score-2026-06-12.pdf` and `audits/full-audit-2026-06-12.md`. Aggregate **74/100 (Good)**. Dimensions: SEO 74, GEO 71, Perf 67, A11y 80, Trust 87, Content 63. ⚠️ One manual-action risk surfaced: home-page `aggregateRating.reviewCount` "19" vs 12 reviews actually rendered (`src/app/(en)/page.tsx`) — fix first. Lowest dims are Perf (4 oversized gallery WebP + missing hero preload + 4 blog heroes missing `priority`) and Content (5 of 9 blog posts missing from `sitemap.ts`). Security verdict: no secrets, secure for stack; two HIGH header gaps (Permissions-Policy, HSTS). Read-only audit — nothing fixed yet.
- [x] 2026-06-12 — **First real-job content shipped via new `job-to-content` skill** — Mays Landing textured-ceiling job (start-to-finish: popcorn removal → tear-out to joists → new drywall → knockdown re-texture). Triaged 13 phone JPEGs through the skill's postability gate (no faces/addresses — all clean), selected 4, converted to WebP q80 at 1536px (~3.0MB → 685KB) into `public/projects/popcorn-ceiling-removal-{before,demo,drywall,after}-mays-landing-nj.webp`. Placed as a "Recent Project" figure on **two** pages with image schema and `next/image`: the `popcorn-ceiling-removal` service page (4-frame process strip, added `image[]` to its `Service` JSON-LD) and the `painters-mays-landing-nj` city page (2-frame before/after via a new optional `project` field on the `ServiceArea` data type — captions intentionally **unique per page** to avoid doorway signals; image schema added to the city `Service` node only when a project renders). First images on either page. Typecheck clean. GBP post / IG before-after caption / customer review-request drafted as copy for BP to post (off-site). This is flywheel #2 (real job → geo-tagged proof) of the client lead engine.
- [x] 2026-05-25 — **Built `wrap-report` skill** (`<websites>/.claude/skills/wrap-report/`) — the missing client-facing monthly/quarterly deliverable that bundles three lanes in one PDF: (1) what we shipped this period (green, pulls from PROGRESS `## Shipped` + audit-tracker closed-in-period findings), (2) what the client owes us (amber, pulls audit-tracker findings with `actor in {client, both}` — GBP claim, Yelp NAP sync, BBB listing, review asks, photo refreshes, project photos for gallery), (3) our forward roadmap (blue, phases the open `actor=us` findings by effort/severity into "Quick wins" / "Content batches" / "Architectural" with projected score lifts from the site-score rubric). Cover shows current site-health score + projected after-roadmap number. Same Ember Tech palette as fix-report + site-score. Extended `audit-tracker` to support `actor` field (us | client | both), added `external` category for off-site/client items, added `add` and `set-actor` modes. Period selectable per run: `--month YYYY-MM` | `--quarter QN-YYYY` | `--from / --to` (BP chose "ask each run" for default cadence). First real wrap report rendered at `audits/wrap-report-2026-05.pdf` — Perfect Finish May 2026, current score 98, 6 client items seeded.
- [x] 2026-05-25 — Seeded 6 standard client-side findings in PF ledger (`external.gbp-claim`, `yelp-nap-sync`, `bbb-listing`, `review-asks`, `gbp-photo-refresh`, `project-photos`) — all `actor=client`. The GBP/Yelp/BBB consistency item that was previously in PROGRESS Next-up section is now in the ledger and rendered into Lane 2 of the wrap report. PROGRESS Next-up will be slimmed in a separate pass once the ledger fully owns these.
- [x] 2026-05-25 — **Built `audit-tracker` skill + backfilled Perfect Finish ledger.** New skill at `<websites>/.claude/skills/audit-tracker/` — Python helper (`scripts/tracker.py`) backs a per-project `audits/findings.json` ledger. Six modes: `init`, `ingest` (diffs new vs ledger, auto-closes findings that disappear, blocks re-ingest of known audit IDs as a safety against stale-data wipeouts), `close` / `dismiss` / `reopen`, `report` (one-screen burn-down), `burn-down` (renders markdown table to `audits/burn-down-<date>.md`). Stable finding IDs follow `<category>.<check-slug>.<page-slug>` so an SEO finding for cabinet-painting is the same row across every audit run, ever. Categories whitelisted (seo|geo|perf|a11y|security|content|schema|trust|infra) — category drift breaks queryability. Backfilled the May 22 SJ Tech audit (12 findings, score 83): closed 8 from the May 23 audit batch (commit `78ec959`) + 2 from the May 24 polish session, dismissed 2 a11y findings as false-positive (gallery `fill`-mode images don't take width/height attrs). Two LOW findings still open (`seo.meta-description-length.various`, `seo.broken-external-link.unknown`). First burn-down at `audits/burn-down-2026-05-25.md`. Categories I'll standardize on going forward: ledger is the source of truth for "what's open", PROGRESS.md stays for narrative.
- [x] 2026-05-24 — **Fixed the two drags from this morning's site-score**, then re-ran the score. (1) `/privacy` updated — "Information We Collect" and "Who We Share With" sections now explicitly disclose Vercel Analytics alongside GA + Meta Pixel (was a real gap — `(en)/layout.tsx` imports `@vercel/analytics/next` site-wide). (2) Added two captioned `<Image>` figures to each of the 4 blog posts that previously rendered only their hero image: `best-exterior-paint-south-jersey` (deck-exterior hero + siding-staining mid-content), `cedar-siding-repair-south-jersey` (siding hero + shed-exterior mid-content), `exterior-painting-south-jersey` (front-door hero + door-refinishing mid-content), `difference-between-flat-and-satin-paint-finish` (living-room hero + bathroom-satin mid-content). All use `next/image` with width/height and figcaption; mid-content figures get `loading="lazy"`. Hero `figcaption` matches the Article JSON-LD `image` field already declared. Required adding `import Image from "next/image"` to all 4 blog files. tsc clean. **Re-scored:** Trust 97→100, Content 92→100. Aggregate **96 → 98**. PDF re-rendered at `reports/perfect-finish-painters-site-score-2026-05-24.pdf`.
- [x] 2026-05-24 — **Updated fix-report deliverable** to reflect the SJ Tech site-health score. Swapped "Critical audit failures closed: 4 → 0" metric for the headline "SJ Tech site-health score: 83 → 98 / 100" — that score now implicitly summarizes the four critical failures the audit batch closed. Added a 6th fix (Privacy policy / Vercel Analytics disclosure). New PDF at `audits/perfect-finish-painters-fix-report-2026-05-24.pdf` + JSON at `audits/perfect-finish-painters-fix-report-2026-05-24.json` (May 23 version preserved for the record).
- [x] 2026-05-24 — Generated site score `reports/perfect-finish-painters-site-score-2026-05-24.pdf` (initial run, before today's fixes) — aggregate **96/100 (Excellent)**. Dimensions at that moment: SEO 99, GEO 91, Perf 98, A11y 97, Trust 97, Content 92. The 96 vs. 2026-05-12 run's 98 wasn't a code regression — two stricter checks newly caught: Vercel Analytics privacy disclosure missing and 4/5 blog posts lacking inline body images. Both fixed same session — see entry above.
- [x] 2026-05-23 — **Audit batch (TTFB + schema + content + CSP) — commit `78ec959`.** SJ Tech 5/22 audit (83/100) flagged 4 fails — root-caused in `SITE_REPORTS.md` 2026-05-23, then executed. **(1) TTFB 2296ms fix:** root cause was `await headers()` in `src/app/layout.tsx` (lang-detection from `x-pathname` set by middleware), which forced every page into dynamic rendering. Solution: proper Next 16 multi-root-layout refactor — moved all 30 English route directories into `src/app/(en)/` group with its own `<html lang="en">` layout, moved 4 Spanish `pintores-*` directories into `src/app/(es)/` group with `<html lang="es">` layout, deleted the old root `layout.tsx`. Cleaned up the now-orphaned `x-pathname` setter in `src/middleware.ts`. URLs unchanged (route groups don't affect paths), sitemap unaffected. Build now shows **59/59 pages as `○ Static`** (previously all dynamic) — expected TTFB drops from 2.3s to <100ms. **(2) Schema errors (79 LocalBusiness, 346 total):** regenerated `HousePainter` JSON-LD in both new layouts with the two missing-required fixes the audit was tripping on — added `address` (PostalAddress, locality-only Mays Landing/NJ/08330/US — Google's rich-results validator requires the field even for service-area businesses), and added `provider: { @id }` to every nested `Service` inside `hasOfferCatalog.itemListElement`. Closes ~50 missing-address errors (one per page that renders the entity) + ~250 nested-Service warnings. **(3) Content length (26/50 pages <500):** added a `localFaqs` field to `ServiceArea` type + 3 city-specific FAQs per city (pricing / timeline / local-character question) across all 19 cities in `src/data/service-areas.ts` — pricing answers reference local pricing band, timelines vary by city size, third question is genuinely local (coastal-paint Q for shore cities, plaster-prep Q for historic, Spanish Q for Pleasantville). Updated `[city]/page.tsx` template to render the FAQ section as `<h3>` Q + `<p>` A with brand-blue left border, AND emit a matching `FAQPage` JSON-LD. Lifts each city page from ~450 words to ~700-800 words (clears the 500-word threshold) AND adds 19 new FAQPage entities to the structured-data graph. **(4) Thin content:** `/our-work` got a 250-word "How Every Perfect Finish Project Comes Together — prep / prime / paint / protect" section between hero and gallery (was ~120 words). `/blog` index got a 150-word intro paragraph between hero and the post grid (was ~280). **(5) Warnings:** added a real **Content-Security-Policy** header in `next.config.ts` listing the actual third parties (GA, FB Pixel, Vercel Analytics, Google Maps iframe, FB connect) plus the standard hardening (`frame-ancestors 'none'`, `object-src 'none'`, `upgrade-insecure-requests`); `'unsafe-inline'` on script-src + style-src kept because Next 16 emits inline hydration scripts and Tailwind injects inline styles. **Used the `/schema-generator` skill** for the canonical `HousePainter` reference per audit-report recommendation. **Deferred from the audit batch:** meta-description tweaks (actual `description` lengths all measured 130-160 chars by recount — audit tool likely flagged shorter OG/Twitter descriptions on a few pages, low-impact polish), broken-external-link identification (1 of facebook/instagram/yelp/g.page — needs live `curl -I` check), and the alt/dim image audit (likely mostly false positives from `<Image fill>` rendering without width/height attrs and decorative `alt=""` — `OurWorkGallery.tsx` alone has 36 `fill` images which the audit naïvely counts as missing dims). `npx tsc --noEmit` clean. `npm run build` passes, **59/59 static pages generated.** Audit findings + executed fixes both written up in `SITE_REPORTS.md`.
- [x] 2026-05-15 — GEO H2-as-questions sweep extended to 8 pages missed in the 2026-04-26 batch. Rewrote 13 statement H2s as questions across: `/about` ("Why Choose Perfect Finish Painters?"), `/accessibility` ("What Have We Done?" + "How Do We Test?"), 4 blog posts (best-exterior-paint, cedar-siding-repair, exterior-painting, sherwin-williams-snowbound — section H2s inside `dangerouslySetInnerHTML` strings), `/` home ("What Painting Services Do We Offer?", "How Does It Work?", "Which Areas Do We Serve?"), `/services` ("How Does It Work?", "Which Areas Do We Serve?"). Site-wide H2 question ratio should creep further past the 48% mark from the prior sweep. tsc clean.
- [x] 2026-05-15 — Added 4 new Galloway drywall+paint project shots to `OurWorkGallery.tsx`. Source: 5 jpegs BP dropped in `pictures/` (one duplicate dropped). Converted to webp at q82 via sharp, named per gallery convention. Splits across 2 categories so both filters surface the same job: **Drywall** — `drywall-installation-studs-to-hung-galloway-nj.webp` (studs → drywall hung), `drywall-finishing-taping-to-painted-galloway-nj.webp` (taped → primed paint-ready); **Interior** — `studs-to-finish-room-renovation-galloway-nj.webp`, `framing-to-finished-drywall-paint-galloway-nj.webp` (both full-transformation shots). Loose jpegs + `pictures/` folder + one-shot conversion script cleaned up. tsc clean.
- [x] 2026-05-15 — NJ HIC license number updated `13VH13371500` → `13VH14120900` (real license, previous was a placeholder). Swept 4 files: `Footer.tsx`, `contact/page.tsx` (badge), `faq/page.tsx` (licensed-and-insured FAQ answer), `commercial-painting/page.tsx` (FAQPage schema answer).
- [x] 2026-05-12 — Site score run #5: aggregate **97 -> 98 (+1)**. SEO 96 -> 99 (all 36 page titles now 30-60 chars + all 35 metas 130-160). Other dimensions unchanged: GEO 95, Perf 100, A11y 97, Trust 100, Content 95. PDF at `reports/perfect-finish-painters-site-score-2026-05-12.pdf`. Remaining lever to ~99: the per-page heading-hierarchy audit (A11y -3) + nudging the H2 question ratio past 50%.
- [x] 2026-05-12 — **Session 6 (title/meta length) done — 16 titles + 12 descriptions rewritten.** Titles trimmed from 61-113 chars down to 46-59 (e.g. cedar-siding blog 94->52, exterior-painting blog 109->59, sherwin-williams blog 113->55, /services 90->56, /about 76->56). Descriptions trimmed from 161-208 down to 145-160 (e.g. /pintores-cerca-de-mi 208->156, /services 202->153, /faq 193->154); /terms expanded 120->150. Edits scoped to each file's `metadata` block so JSON-LD `headline`/`description` and visible `<h1>`s weren't touched (though the metadata change does propagate to matching og/twitter titles+descriptions, keeping them in sync). tsc clean, lint clean, build passes.
- [x] 2026-05-12 — **"Thin content on 30 pages" Next-up item RETIRED — it was a stale audit false positive.** Ran a word-count audit across all 36 route pages: only `/quiz` is under 300 words and that's a redirect page (correctly thin). Every real page is 500-3000 words (service pages 1100-1900, blog posts 1000-3000, city template 772, legal pages 1200-1700). The old audit tool was almost certainly counting JSX/SVG markup as non-content and undercounting. No content-bulking needed; the lighter pages (`/blog` index 522, `/flooring` 583, `/reviews` 628, `/our-work` 704) are light by nature (index/gallery/review-list) and don't need padding. Same pattern as the Sean Patrick "template-swap garbage" diagnosis that turned out stale.
- [x] 2026-05-11 — Site score run #4 (after quick-win polish batch): aggregate holds at **97/100 (Excellent)**. Dimensions: SEO 96, GEO 95, Perf 100, A11y 97, Trust 100, Content 95. Today's batch was polish, not score-movers — the remaining levers are Session 6 (title/meta length cleanup on ~15 pages, thin content on ~30 pages) and the heading-hierarchy audit (A11y -3). PDF at `reports/perfect-finish-painters-site-score-2026-05-11.pdf`.
- [x] 2026-05-11 — **Quick-win polish batch.** (1) `<a href="/...">` → `<Link href="/...">` on the 4 internal links in `[city]/page.tsx` and the 1 in `drywall-repair/page.tsx` — clears the two pre-existing `<a>`→`<Link>` lint errors (tel: links correctly left as `<a>`). (2) `&apos;` HTML entities → real apostrophes inside JS string literals: 18 occurrences across `blog/page.tsx`, `blog/cedar-siding-repair-south-jersey`, `blog/exterior-painting-south-jersey` (metadata `title`/`description`/og/twitter — these were serializing `&apos;` *literally* into `<title>`/`<meta>` tags) and `deck-staining/page.tsx` (step `desc:` strings rendered as text). JSX text-node `&apos;` left alone — those satisfy `react/no-unescaped-entities` and are correct. (3) Blog `Article.image` JSON-LD repointed off `/logo.webp` onto representative photos: best-exterior-paint→deck-exterior, cedar-siding-repair→exterior-siding-staining, flat-vs-satin→living-room-interior, exterior-painting→exterior-door (sherwin-williams already pointed at its own hero). (4) Fixed pre-existing breadcrumb on the exterior-painting blog post ("Enhance Your Home's Curb Appea" → "...Curb Appeal", apostrophe escaped) — clears the last `react/no-unescaped-entities` error. **Skipped from the menu:** the "100+ Projects Completed" city-page stat (projects ≠ reviews — not actually a mismatch, plausible for an established painter, not schema so no Google risk) and the `middleware.ts`→`proxy.ts` rename (Next already maps it transparently — build output shows "ƒ Proxy (Middleware)" — renaming the file now is premature; Next 16 isn't released). tsc clean, lint clean (1 unavoidable warning: the FB Pixel `<noscript>` 1×1 beacon must be raw `<img>`), build passes.
- [x] 2026-04-26 — Site score run #3 (after FAQ schema-content fix): aggregate **95 → 97 (+2)**. GEO climbed 85 → 95 (+10) because the FAQPage schema/visible-content match ratio went 7/20 → 20/20. Same-day daily total: **91 → 95 → 97 (+6 across 3 runs)**. Five of six dimensions now ≥95.
- [x] 2026-04-26 — **FAQ schema-without-content manual-action risk eliminated** across 13 pages (home + 12 service pages: cabinet, commercial, deck, drywall, epoxy, exterior, fence, garage-door, interior, popcorn-ceiling, shed-restoration). Audit script confirmed only 7 of 20 FAQPage-bearing pages were rendering visible Q&A matching their schema (`/faq`, `/ceiling-painting`, `/wallpaper-removal`, 4 pintores pages — all using their own card patterns). Refactored `src/components/FAQSection.tsx` to accept `items` + optional `heading`/`description` props (was hardcoded with stale FAQs that didn't match any schema), updated `/services` to pass its existing `paintFaqJsonLd.mainEntity`, then swept the 13 at-risk pages with: import + `speakable` property added to faqJsonLd + `<FAQSection items={faqJsonLd.mainEntity} />` rendered before `<Footer />`. Schema and visible content now provably match on every page; speakable schema for AI/voice extraction also applies to all 13 + the existing /faq. tsc clean.
- [x] 2026-04-26 — Speakable schema shipped on `/faq` (the only page with confirmed visible Q&A matching its FAQPage schema). Added `speakable: { "@type": "SpeakableSpecification", cssSelector: [".speakable-q", ".speakable-a"] }` to the FAQPage JSON-LD and tagged the question `<span>` and answer `<div>` with matching classes (`src/app/faq/page.tsx:159`, `:225`, `:235`). Voice assistants and AI tools now know which DOM nodes to read aloud when answering audio queries about painting in South Jersey. Expected GEO score: 85 → 90. tsc clean.
- [x] 2026-04-26 — Re-ran site score after A11y + GEO pushes: aggregate **91 → 95 (+4)**. Dimension changes: A11y 77 → 97 (+20, skip-nav + :focus-visible cleared two –10 drags), GEO 80 → 85 (+5, H2 question ratio lifted 24% → 48% via 43-H2 sweep). Other dimensions unchanged. PDF re-rendered at `reports/perfect-finish-painters-site-score-2026-04-26.pdf` (overwrote earlier same-day snapshot; both score values preserved in this PROGRESS.md trail).
- [x] 2026-04-26 — GEO H2 sweep across 13 service pages: 43 statement H2s rewritten as questions ("How Much Does X Cost in Atlantic County, NJ?", "What's Included in X?", "How Do You Y? Our Process", "Where Do We Offer X?", "What Other Services Do We Offer?", etc.). Skipped CTA-style headings ("Make Your Fence Look New Again", "Bring Your Shed Back to Life") and FAQ section labels (already imply Q&A). Site-wide question ratio: 24% → 48%. Remaining 84 statement H2s are legal page section labels, blog editorial headings, and short nav labels — diminishing returns to convert. Expected GEO score: 80 → ~85. tsc clean.
- [x] 2026-04-26 — A11y push (most of Session 4): added a skip-nav link to root layout (`layout.tsx:194` — anchored to `<main id="main-content">`) and `:focus-visible` keyboard focus rings + skip-link styles in `globals.css`. Skip-link is hidden offscreen until tab-focused, then slides into view at top-left. Focus rings honor user input modality (mouse clicks don't trigger; keyboard tab does). Expected score impact: A11y 77 → ~97, aggregate 91 → ~94. tsc clean. Heading hierarchy formal audit still pending.
- [x] 2026-04-26 — Generated site score `reports/perfect-finish-painters-site-score-2026-04-26.pdf` — aggregate **91/100 (Excellent)**. Dimensions: SEO 95, GEO 80, Perf 100, A11y 77, Trust 100, Content 95. Lowest dimension (A11y) is dragged by missing skip-nav link, no `:focus-visible` styles, and unaudited heading hierarchy — addresses Session 4 Next-up items.
- [x] 2026-04-26 — Page performance audit on `/quiz` conversion funnel. Found `src/app/quiz/page.tsx:25` does a server `redirect()` to `https://estimate-app-liart.vercel.app/quiz/perfect-finish-painters` — every "Get Free Estimate" click (55 CTAs site-wide) eats a 307 + a fresh DNS/TCP/TLS handshake to a different domain (~150–600ms cost before the quiz starts loading). Auto-fix shipped: added `<link rel="preconnect">` + `<link rel="dns-prefetch">` for `estimate-app-liart.vercel.app` in root layout `<head>`, warming the connection so the redirect doesn't pay a cold handshake. tsc clean. Architectural fix (eliminating the redirect entirely) tracked in Next up.
- [x] 2026-04-26 — GEO audit (`/geo-ai-optimize`) on home + interior-painting + city template; shipped fixes:
  - **Service schema `areaServed` mismatch fixed.** All 14 service pages were declaring `areaServed: { "@type": "AdministrativeArea", name: "South Jersey, NJ" }` (single generic area) while the root `HousePainter` listed only 14 of the 19 cities in `serviceAreas`. Added `areaServedCities` export to `src/data/service-areas.ts` (derives `[{ "@type": "City", name }]` from the data file), refactored `layout.tsx` + all 14 service pages (cabinet/ceiling/commercial/deck/drywall/epoxy/exterior/fence/flooring/garage-door/interior/popcorn-ceiling/shed/wallpaper) to consume it. Net: single source of truth, root LocalBusiness coverage widened to all 19 cities (previously 14), every Service schema now matches.
  - **Question-based H2s for AI extraction.** Rewrote 5 statement headings as questions: `interior-painting/page.tsx` ("Interior Painting Services in Atlantic County, NJ" → "What Interior Painting Services Do We Offer in Atlantic County?", "What's Included" → "What's Included in an Interior Painting Job?", "Our Interior Painting Process" → "How Do You Paint a Room? Our 4-Step Process") and `[city]/page.tsx` template (4 H2s reworded — propagates to all 14 city pages).
  - tsc clean.
- [x] 2026-04-26 — Page performance audit (`/page-performance`) on `https://perfectfinishpainter.com`. Site is in excellent shape — all images use `next/image`, hero LCP has `priority`+`fetchPriority="high"` (`page.tsx:224-232`), fonts via `next/font` (Geist) so `display:swap` + preload are automatic, GA + FB Pixel both `afterInteractive`, zero raw `<img>` tags, zero `.jpg`/`.jpeg`/`.png` source references. Auto-fix: deleted two orphan files in `public/` (`download--4-.jpeg` 1.3MB + `download--4-.webp`, both verified unreferenced anywhere in source or public). `public/` total dropped 36MB → 34MB.
- [x] 2026-04-26 — Killed Google Translate widget. Symptom: ES button silently no-op when clicked before widget JS finished loading (`afterInteractive` strategy + cross-frame iframe access in `toggleSpanish()` were both racy), and translation roundtrip was slow even when it worked. Replaced ES button with a `<Link>` that routes to the matching hand-written Spanish page (`/painters-pleasantville-nj` → `/pintores-pleasantville-nj`, `/painters-atlantic-city-nj` → `/pintores-atlantic-city-nj-es`, `/painters-vineland-nj` → `/pintores-vineland-nj`) and falls back to `/pintores-cerca-de-mi` everywhere else. On `/pintores-*` pages the same button reverses to "EN" and links back to the English equivalent (or `/`). Removed `<div id="google_translate_element">`, the two `translate.google.com` Script tags from `layout.tsx`, and the `.goog-te-*` overrides from `globals.css`. tsc clean.
- [x] 2026-04-25 — Added `www.` → apex 301 redirect in `src/middleware.ts` (code-level fallback in case Vercel domain config drifts). Existing query-strip 301 already kills `/?q={search_term_string}` stragglers from old SearchAction schema. Both fixes target Search Console hits: `http://www.perfectfinishpainter.com/` and `https://perfectfinishpainter.com/?q={search_term_string}`. tsc clean.
- [x] 2026-04-22 — Session 3: image optimization. Wrote one-shot `scripts/convert-to-webp.mjs` (sharp, quality 82) and converted 55 raster files in `public/` + `public/before/` + `public/images/blog/` to `.webp` siblings. Regenerated `logo.webp` from the higher-quality `logo.png` source. Then `scripts/retarget-images.mjs` swept `src/**/*.{ts,tsx}` and rewrote 193 URL references (`.jpg`/`.jpeg`/`logo.png` → `.webp`) across 41 files, including OpenGraph/Twitter image metadata and Article JSON-LD `image` fields. Originals left in place as fallbacks (cacheable under existing static asset rule). Total image bytes dropped ~24MB → ~12MB (~50%). Alt text audit: every `<Image>` already had an alt set in source; 92/286 "missing" in audit report likely counted inline SVG icons or OG image references — no source-level action needed. Width/height + lazy-loading are handled automatically by `next/image`. Build passes, 59 routes compile.
- [x] 2026-04-22 — Header now links to `/contact` and `/faq` (desktop nav + mobile menu). `ContactForm.tsx` mailto repointed from `perfectfinishpainterr@gmail.com` → `info@perfectfinishpainter.com` (BP's real inbox).
- [x] 2026-04-22 — Session 2: legal/essential pages. Created `/contact` (NAP + methods + map + `ContactPage` schema merging by @id), `/privacy` (GA/Meta/Vercel disclosure, retention, rights), `/terms` (estimates non-binding, payment, warranty, NJ governing law), `/accessibility` (WCAG 2.1 AA target, known limitations, feedback channel), `/faq` (20 Qs across Pricing, Scheduling, Prep, Warranty, Service Areas with `FAQPage` schema). Footer restructured into 5-column layout with new Legal section. Sitemap extended with all 5 new routes. Expected audit impact: E-E-A-T 50→~75 (Contact + Privacy FAILs cleared), Content Analysis 40→~65 (Essential Pages FAIL cleared), Conversion Architecture Footer warning cleared.
- [x] 2026-04-22 — Session 1 schema fixes (audit 2026-04-22, LocalBusiness 75 errors / 415 total)
  - Root `HousePainter` in `src/app/layout.tsx`: removed `address` (service-area business), removed `aggregateRating`+`review` (was rendering on 48 pages with no visible review content), removed 1-item `BreadcrumbList`, removed duplicate `Organization` entity, added stable `@id` (`#business`), dropped `", NJ"` from `areaServed` city names, normalized `dayOfWeek` to arrays, flattened `hasOfferCatalog` (removed redundant `ListItem` wrapping `Offer`), added `contactPoint` and `logo`, linked `WebSite` → business via `@id`
  - `src/app/[city]/page.tsx` (14 city pages): removed duplicate `HousePainter` (had `streetAddress: "Mays Landing"` which is a city not a street), replaced with a `Service` entity that references the root business by `@id` and carries per-city `areaServed` with `geo` + `containedInPlace`
  - `src/app/page.tsx`: added `HousePainter` JSON-LD with `aggregateRating` + 5 `review` entries, merged by `@id` into root — home renders `<ReviewsSection />`
  - `src/app/reviews/page.tsx`: added `HousePainter` JSON-LD with `aggregateRating` + full `review` array (built from `reviews` data) — page renders all review cards
  - `src/app/blog/cedar-siding-repair-south-jersey/page.tsx` + `src/app/blog/exterior-painting-south-jersey/page.tsx`: replaced literal `&apos;` inside JSON-LD `headline`, `BreadcrumbList` item name, and `description` with real `'`
  - Verified: `npx tsc --noEmit` passes; lint errors all pre-existing (no new ones from this session)

## In progress
- [ ] Session 1 & 2 verification — BP to run Google Rich Results Test on home, `/reviews`, a city page, a blog page, and `/faq`; skim `/contact`, `/privacy`, `/terms`, `/accessibility` for tone/wording changes before next audit run

## Next up
- [ ] **Reduce the cross-domain quiz redirect cost.** The estimate app at `estimate-app-liart.vercel.app/quiz/perfect-finish-painters` is **Senku Labs** (BP's multi-tenant lead-management product, `senkulabs.com`) — quiz submissions feed Senku's lead pipeline. **Do NOT fold the quiz into this app** — that breaks Senku's multi-tenant design (other client sites will route there too). Acceptable paths: (a) custom subdomain `quiz.perfectfinishpainter.com` pointing at the Senku Vercel project — same parent domain saves a DNS handshake and kills the unbranded `vercel.app` URL bar, (b) change all 55 internal CTA links to point at the destination URL directly (saves the redirect roundtrip but loses canonical /quiz URL for backlinks/GSC tracking), or (c) keep the preconnect+dns-prefetch stopgap that's already shipped if the perf cost is acceptable.
- [ ] **Verify GBP / Yelp / BBB / Angi listings carry the identical name + description + service list as the website schema.** Biggest remaining GEO win — AI ingests inconsistent local listings as separate entities and dilutes citations. (External work, can't be done from the codebase.)
- [ ] **Run the 12 baseline GEO test queries** in ChatGPT, Perplexity, Google AI Overviews, and Microsoft Copilot. Log which queries cite Perfect Finish vs competitors (CertaPro etc.) vs generic Yelp/HomeAdvisor pages. Re-run monthly. Queries: best painters in Mays Landing NJ; interior painters near EHT NJ; how much does interior painting cost in Atlantic County NJ; how long does it take to paint a room in South Jersey; drywall repair Mays Landing NJ; best exterior painters Atlantic County NJ; painters who use Sherwin-Williams in South Jersey; cabinet painting near Galloway NJ; painters in Somers Point NJ that speak Spanish; compare Perfect Finish Painters vs CertaPro Mays Landing; best painters for new construction homes in Egg Harbor Township; Perfect Finish Painters reviews.
- [ ] **`reviewCount: 5` in root aggregateRating doesn't justify "100+ Projects Completed" stat on city pages** (`[city]/page.tsx:236`). Either back the 100+ claim with a real number/recent-projects section, or change the stat to match what's substantiated.
- [ ] **Decide on the legacy `.jpg` fallback library** — `public/` still ~34MB after orphan cleanup; most of that is intentionally-kept `.jpg` originals from the WebP migration (locked decision). They don't hurt page load (nothing references them), but they bloat every Vercel deploy + `next build` time. If no external sites or social platforms still hot-link the old `.jpg` URLs, move them out of `public/` into a local archive folder.
- [ ] If audit alt/width/lazy numbers persist after next re-crawl: audit tool may be counting inline SVG icons. Add `aria-hidden="true"` to decorative SVGs and `<title>` to informative ones.
- [ ] Session 4 (accessibility) remainder: heading hierarchy formal audit per page (skip-nav + `:focus-visible` shipped 2026-04-26; ContactForm labels already present)
- [ ] Session 5 (performance): TTFB 1757ms (static where possible), LCP, CSP header, HTTP/2 confirm
- [ ] Session 7 (cleanup): question-based headings on remaining pages (push H2 ratio past 50%). Note: `<a>`→`<Link>`, `Article.image` per-post, and metadata `&apos;` all done 2026-05-11. `middleware.ts`→`proxy.ts` rename deferred — Next maps it transparently today (build shows "ƒ Proxy (Middleware)"); revisit when Next 16 ships.

## Locked decisions
- Perfect Finish is a service-area business — `HousePainter` schema uses `areaServed` only, no `address` or `streetAddress`. Do not re-add a physical address.
- Single source of truth for the business entity: `@id: "https://perfectfinishpainter.com/#business"`. Pages that add properties (reviews, ratings) reference this `@id` and are merged, not duplicated.
- `aggregateRating` + `review` may only appear on pages that actually render the review cards (currently home + `/reviews`). Do not add them to layout or other pages — Google treats this as a manual action risk.
- Public contact email is `info@perfectfinishpainter.com` — confirmed BP's real inbox. `ContactForm.tsx` submissions now route there too (was `perfectfinishpainterr@gmail.com`, changed 2026-04-22).
- `/faq` is the canonical FAQ location. Individual service pages keep their own `FAQPage` schema for topic-specific questions, but the footer and primary nav should link to `/faq`.
- WebP is the canonical image format site-wide. Source `.jpg`/`.jpeg`/`.png` originals are kept in `public/` as cached fallbacks; do not delete them without checking for stale references first. If adding new images, drop both `.jpg` (or `.png`) + `.webp` alongside, or re-run `node scripts/convert-to-webp.mjs` and reference the `.webp`.

## Don't redo
- Don't put a `HousePainter` on `[city]/page.tsx` — the root layout already emits one, and duplicate LocalBusiness entities on one page conflict.
- Don't set `streetAddress: "Mays Landing"` — "Mays Landing" is a city, not a street. The fix is to remove the address block, not correct the value.
- Don't use `&apos;` inside plain JS string values that get passed to `JSON.stringify` — the entity gets serialized verbatim and Google sees `Home&apos;s` instead of `Home's`. `&apos;` is only for JSX text nodes / HTML attributes.
