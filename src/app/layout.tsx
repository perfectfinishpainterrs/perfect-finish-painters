import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { Analytics } from "@vercel/analytics/next";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Perfect Finish Painters | Professional Painting Services in South Jersey",
  description: "Transform your space with Perfect Finish Painters. Professional interior & exterior painting, drywall repairs, and flooring services in Mays Landing and South Jersey. Get a free estimate in 60 seconds!",
  keywords: "painting, painters, Mays Landing, South Jersey, interior painting, exterior painting, drywall repair, flooring",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
        <Analytics />
      </body>
    </html>
  );
}
